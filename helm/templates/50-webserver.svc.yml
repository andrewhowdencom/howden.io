---
# Generated by boilr at Thu, 27 Oct 2016 21:18:20 CEST
kind: "Service"
apiVersion: "v1"
metadata:
  name: "webserver"
  namespace: "howden-io"
  
  labels:
    application: "nginx"
    role: "webserver"
spec:
  selector:
    application: "nginx"
    role: "webserver"
  ports:
    - protocol: "TCP"
      name: "http"
      port: 80
    # - protocol: "TCP"
    #   name: "another-http"
    #   port: 30http
  type: "NodePort"
